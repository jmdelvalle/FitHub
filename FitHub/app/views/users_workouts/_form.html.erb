<%= form_for(@users_workout) do |f| %>
  <% if @users_workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_workout.errors.count, "error") %> prohibited this users_workout from being saved:</h2>

      <ul>
      <% @users_workout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
