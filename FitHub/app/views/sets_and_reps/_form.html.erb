<%= form_for(@sets_and_rep) do |f| %>
  <% if @sets_and_rep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sets_and_rep.errors.count, "error") %> prohibited this sets_and_rep from being saved:</h2>

      <ul>
      <% @sets_and_rep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
