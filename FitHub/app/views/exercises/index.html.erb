<p id="notice"><%= notice %></p>

<h1>Listing Exercises</h1>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <%= collection_check_boxes(:exercise, :name, Exercise.all, :id, :name) do |b| %>
    <%= b.label(:"name" => b.value) { b.check_box + b.text } %><br>
    <% end %>

    <% @legs = @exercises.find_by_muscle_group("Legs") %>
    <%= @legs.name do %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Exercise', new_exercise_path %>
